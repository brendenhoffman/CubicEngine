#version 460

// No-op: exists purely to validate the compute pipeline path (creation,
// dispatch recording, compute<->graphics barriers) before any real compute
// shader (terrain gen, chunk meshing, culling) is written.
layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {}
