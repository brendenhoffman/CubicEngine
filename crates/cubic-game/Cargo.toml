[package]
name = "cubic-game"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
noise = { workspace = true }
toml = { workspace = true }
serde = { workspace = true }
